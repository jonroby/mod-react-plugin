const component = require('./component');
const reducer = require('./reducer');
const request = require('./request');
const saga = require('./saga');

module.exports = { component, reducer, request, saga };
